spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:dev-database.itau.com}:${DB_PORT:5432}/${DB_NAME:transaction_db}
    username: ${DB_USER}
    password: ${DB_PASS}
  
  jpa:
    hibernate:
      ddl-auto: validate
  
  sql:
    init:
      mode: never

  cloud:
    aws:
      sqs:
        endpoint: null
